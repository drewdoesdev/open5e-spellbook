import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      height: 55,
      width: '100%'
    },

    titleWrapper: {
        padding: 15,
        borderBottomWidth: 1,
        borderColor: "#333"
    }
  });